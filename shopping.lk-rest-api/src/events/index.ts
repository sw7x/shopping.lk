// Emitting an event

//userEmitter.emit('userCreated', { name: 'John Doe', age: 30 });
//userEmitter.emit('userDeleted', { name: 'abc', age: 99 });

//myEventEmitter.emit('myEvent', arg1, arg2); // passing arguments down to the listener
